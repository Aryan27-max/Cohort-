📘 GIT COMMANDS — WELL ORGANISED NOTES
🔗 REMOTE REPOSITORY (ORIGIN)
✅ Check connected repo
git remote -v

🔁 Change existing origin
git remote set-url origin <repo-link>

📤 COMMIT & PUSH (EXISTING REPO)
git add .
git commit -m "your commit message"
git push origin main

🆕 CREATE NEW REPO & PUSH CODE
🪜 Step-by-step
cd your-folder
git init
git add .
git commit -m "first commit"
git remote add origin https://github.com/username/repo.git
git branch -M main
git push -u origin main

⚡ QUICK COMMIT SHORTCUT (ALIAS)
Create shortcut once:
git config --global alias.c "commit -am"

Use:
git c "Commit 1"
git c "Commit 2"
git push

📜 VIEW COMMIT HISTORY
git log

⏪ GO BACK TO OLD COMMIT (TEMP VIEW)
git checkout <commit-hash>


👉 To return:

git checkout main

🌿 BRANCH COMMANDS
Switch branch:
git checkout branch-name

Force switch (if stuck):
git checkout -f branch-name

Create & switch:
git checkout -b new-branch

🔄 SYNC LOCAL WITH REMOTE
git pull

🚑 RESET COMMANDS (LIFESAVERS)
🟢 Soft Reset (keep staged changes)
git reset --soft <commit-hash>

🟡 Mixed Reset (keep files but unstaged)
git reset <commit-hash>

🔴 Hard Reset (delete everything after commit)
git reset --hard <commit-hash>

📦 GIT STASH (TEMP SAVE WORK)
git stash
git stash list
git stash apply stash@{0}

🚀 ADVANCED BRANCH CONTROL
🔥 Force push branch → main
git checkout your-branch
git push origin your-branch:main --force-with-lease


✔ safely replaces main with your branch

🧹 Make branch same as main
git checkout your-branch
git fetch origin
git reset --hard origin/main

💻 Make local system same as GitHub
git fetch origin
git checkout main
git reset --hard origin/main
git clean -fd

🔁 REBASE (MOST IMPORTANT WORKFLOW)
👉 What it does:

Moves your commits on top of latest main (removes “behind”)

✅ Rebase cleanly:
git checkout your-branch
git fetch origin
git rebase origin/main

If conflict:
git add .
git rebase --continue

⬆ Push after rebase:
git push --force-with-lease

🏆 PERFECT DAILY DEV FLOW (USE THIS ALWAYS)
git checkout your-branch
git fetch origin
git rebase origin/main
git push --force-with-lease


✔ no behind commits
✔ clean history
✔ no merge mess

📌 GOLDEN RULES

✅ Rebase feature branches
✅ Use --force-with-lease not --force
❌ Don’t force push shared production branches
✅ Reset only when you’re sure