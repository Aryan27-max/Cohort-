git remote -v  ------->  to check the origin of the repo 

git remote set-url origin (link to repo) -----------> sets new origin to the file 

to commit to and existing linked repo ------------>

        git add .

        git commit -m "write the commit"

        git push origin main

to make a new orgin and push a fresh commit ----------->

🪜 STEP 1: Open Terminal in That Folder

        track to the given folder

🪜 STEP 2: Initialize Git 

        git init

🪜 STEP 3: Add All Files & Folders

        git add .

🪜 STEP 4: Commit the Changes

        git commit -m "write a commit"

🪜 STEP 5: Connect to Your GitHub Repo

        git remote add origin https://github.com/your-user-name/repository-name.git

🪜 STEP 6: Push to GitHub (first acess main ranch then add the file to it)

        git branch -M main
        
        git push -u origin main


stack and push git cmds - 

     git config --global alias.c "commit -am"

     git c "Commit 1"
     git c "Commit 2"
     git c "Commit 3"
     git push



-> to check commit logs - 
   
   git log 

-> to retrive to the prev commit of the prev version use 
   1st check the commit logs and find the commit ID -> 

   commit 4279ab98bc47027e42c054722af180c8909f3d93
   Author: Aryan27-max <gupta.raj.aryan.2005@gmail.com>
   Date:   Wed Jan 7 16:47:16 2026 +0530

   intro to licenses

then copy the commit ID and then use -> 

git checkout <commit ID>

-> to switch branches use  

git checout <branch name - Aryan-Fix, main, etc>

-> incase of a headless state force checkout using - 

git checout -f <branch name>

-> Creation of new branch and switching to it 

git branch <branch-name>    |
                            |----> git checkout -b <branch name>
git checkout <branch-name>  |

-> to sync local files to the remote repo use - 

git pull 


